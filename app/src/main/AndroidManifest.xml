<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.mcomella.animationoverview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AnimationSelectionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Exported necessary for run config to load Activity directly. -->
        <activity
            android:name=".view.ViewAnimationInXMLActivity"
            android:exported="true"
            />

        <activity
            android:name=".view.ViewAnimationInCodeActivity"
            android:exported="true"/>

    </application>

</manifest>